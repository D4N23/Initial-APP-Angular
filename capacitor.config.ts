import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'initial.interface',
  appName: 'initial-interface',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
